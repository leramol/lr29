<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Lr29" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="buttonStyle">@style/BaseButton</item>
        <item name="textFillColor">@color/blue</item>
        <item name="background">@drawable/edittext</item>
    </style>
    <style name="BaseButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/blue</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="GreenButton" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/pinkAngry</item>
        <item name="android:textColor">@color/aloe</item>
    </style>

    <style name="GreenTheme2" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="background">@drawable/edittext2</item>
        <item name="android:textColor">@color/cedar</item>
        <item name="android:backgroundTint">@color/moss</item>
        <item name="buttonStyle">@style/GreenButton</item>
       <item name="android:drawableTint">@color/aloe</item>
        <item name="textFillColor">@color/aloe</item>

        </style>
</resources>